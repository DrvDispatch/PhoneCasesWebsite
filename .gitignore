# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env — never commit real secrets
.env
.env*.local
.env.production

# prisma
/prisma/*.db
/prisma/*.db-journal

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/coverage
/legacy/**/node_modules

# Server access / handoff docs — SECRETS, never commit
/SERVER_ACCESS.md
*.local.md
globecase_contabo*

# Google Cloud service account key — SECRET, lives only on the server
/gcp-key.json
